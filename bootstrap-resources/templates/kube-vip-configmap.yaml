apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-vip-config
  namespace: kube-system
data:
{{- range $key, $value := index .Values "kube-vip" "configuration" }}
  {{ $key }}: "{{ $value }}"
{{- end }}
